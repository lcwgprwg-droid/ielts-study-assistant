"use client";
import { useEffect, useState } from "react";
import { Shell } from "@/components/shell";
export default function VocabPage(){const [items,setItems]=useState<Record<string,unknown>[]>([]),[phrase,setPhrase]=useState(""),[meaning,setMeaning]=useState("");const load=()=>fetch("/api/vocab").then(r=>r.json()).then(setItems);useEffect(()=>{ void load(); },[]);async function submit(e:React.FormEvent){e.preventDefault();await fetch("/api/vocab",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phrase,meaning})});setPhrase("");setMeaning("");void load()}return <Shell><header className="topline"><div><div className="eyebrow">Vocabulary collection</div><h1>把值得复用的表达，变成长期记忆。</h1></div></header><form className="card two" onSubmit={submit} style={{marginBottom:20}}><div className="field"><label>词组或单词</label><input required value={phrase} onChange={e=>setPhrase(e.target.value)} placeholder="e.g. keep in touch with"/></div><div className="field"><label>中文释义</label><input required value={meaning} onChange={e=>setMeaning(e.target.value)} placeholder="与……保持联系"/></div><button className="button gold">加入复习库</button></form><section className="card"><div className="card-header"><h2>我的词组库</h2><span className="eyebrow">{items.length} items</span></div>{items.map(x=><div className="issue" key={String(x.id)}><div><b>{String(x.phrase)}</b><small>{String(x.meaning)} · {String(x.topic)}</small></div><span className="pill green">{String(x.reps)} 次复习</span></div>)}</section></Shell>}
